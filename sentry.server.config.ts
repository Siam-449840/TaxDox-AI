/**
 * TaxDox AI — Sentry server config placeholder.
 *
 * This file is intentionally inert until Phase 5.4 installs @sentry/nextjs.
 * It exists so `instrumentation.ts` can reference a path that resolves today
 * (TypeScript checks dynamic-import paths at compile time). When the Sentry
 * SDK is added, replace this body with the real `Sentry.init(...)` call.
 *
 * No-op: Sentry is not initialized yet.
 */
export {}
